<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta http-equiv="X-UA-Compatible" content="IE=edge">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Detalle Producto</title>
</head>

<body class="bg-gray-100">
   <div class="container mx-auto py-8">
      <div class="bg-white p-8 shadow-md rounded-md">
         <h1 class="text-2xl font-bold mb-4">Código: {{this._id}}</h1>
         <h2 class="text-xl font-bold mb-2">Nombre: {{this.title}}</h2>
         <p class="text-gray-700 mb-2">Descripción: {{this.description}}</p>
         <p class="text-gray-700 mb-2">Precio: ${{this.price}}</p>
         <p class="text-gray-700 mb-2">Categoría: {{this.category}}</p>
         <p class="text-gray-700 mb-2">Stock: {{this.stock}}</p>
         <img src="{{this.image}}" alt="{{this.title}}" class="mt-4 rounded-md shadow-md" />
      </div>
   </div>
</body>


</html>