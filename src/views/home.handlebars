<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta http-equiv="X-UA-Compatible" content="IE=edge">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Home</title>
</head>

<body class="bg-gray-100">
   <div class="container mx-auto py-8">
      <div class="bg-white p-8 shadow-md rounded-md mb-8">
         <h1 class="text-2xl font-bold mb-4">¡Bienvenido!</h1>
         <h3><a href="/session/logout" class="text-blue-500 hover:text-blue-700">Cerrar sesión</a></h3>
         <p class="text-gray-700">{{user.name}}</p>
         <p class="text-gray-700">{{user.email}}</p>
         <p class="text-gray-700">{{user.rol}}</p>
      </div>

      <h1 class="text-3xl font-bold mb-4">Lista de productos</h1>
      
      {{#each products}}
      <div class="bg-white p-6 mb-4 ">
         <h2 class="text-xl font-bold mb-2">{{this.title}}</h2>
         <p class="text-gray-700 mb-2">{{this._id}}</p>
         <p class="text-gray-700 mb-2">{{this.description}}</p>
         <p class="text-gray-700 mb-2">${{this.price}}</p>
         <p class="text-gray-700 mb-2">{{this.category}}</p>
         <p class="text-gray-700 mb-2">{{this.stock}} disponibles</p>
         <img src="{{this.image}}" alt="{{this.title}}" class="mb-2 " />
         <button class=" to-blue-500 hover:from-pink-500 " data-product-id="{{this._id}}">Agregar al carrito</button>
         <a href="/product/{{this._id}}" class="text-blue-500 ">Ver detalles</a>
      </div>
      {{/each}}
   </div>
</body>


</html>